List of Files and Directories:

.gitignore	list of files and file extensions which should be ignred by Git
happyfuntokenizing.py	tokenizer script by Chr. Potts specially trimmed to process Twitter data
lib/			custom libraries loaded by different script languages
noise_cleaner		script for replacing textual noiseness effects with their normalized \
                        variant
select_random_line	script for choosing random line from within a specific range
sentence_splitter	script for dividing input paragraph into individual sentences
set_env			shell setup script for adding project's script directories to system PATH
TextTagger		shell script performing all preprocessing and tagging routines
umlaut_restorer		script for translating misspelled writings like ae, oe, ue to \
			legal umlaut characters
